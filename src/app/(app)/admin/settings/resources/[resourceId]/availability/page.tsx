import { ResourceAvailabilityManager } from '@/components/app/admin/settings/ResourceAvailabilityManager';
import React from 'react';

const ResourceAvailabilityPage: React.FC = () => {
  return <ResourceAvailabilityManager />;
};

export default ResourceAvailabilityPage;
